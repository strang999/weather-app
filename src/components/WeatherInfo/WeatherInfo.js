import React from "react";
import { Image } from "semantic-ui-react";

const WeatherInfo = ({
  location,
  temperatureC,
  temperatureF,
  feelsLike,
  icon,
  errorMessage,
  humidity,
  pressure,
  description,
  tempMax,
  tempMin,
  flag,
}) => {
  return (
    <div>
     
    </div>
  );
};

export default WeatherInfo;
